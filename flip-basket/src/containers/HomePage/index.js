import React from 'react'
import Header from '../../components/Header'
import MenuHeadBar from '../../components/MenuHeader'

const HomePage = (props) => {
    return (
        <div>
            <Header />
            <MenuHeadBar />
            <h1>Wellcome to Home Page</h1>
        </div>
    )
}

export default HomePage
